#include <stdio.h>

/**
 * main - prints its name, followed by a new line
 * @argc: numbers sof command line arguments.
 * @argv: arrays that contain the program comman line argument.
 * return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
