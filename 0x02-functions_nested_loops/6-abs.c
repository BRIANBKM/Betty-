#include "main.h"

/**
 * main - main entry
 *
 * Description:
 *
 * Return: integer
 */

int _abs(int b)
{
	if (b > 0)
	{
		return (b);
	}
	else if (b < 0)
	{
		return (-b);
	}
	else
	{
		return (b);
	}
}
